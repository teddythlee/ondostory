# 온도스토리 콘텐츠 운영 가이드

> **온도스토리 글쓰기·SEO 규격의 단일 원본.** 초안 작성은 `ondostory-draft` 스킬이 이 문서를 읽어 따른다. 규격을 바꿀 땐 이 문서만 고친다.

---

## 0. 한 줄 정체성

**미국(특히 OC) 정착·생활을 돕는, 아빠 1인칭 실경험 후기 블로그.**
독자는 "미국/OC에서 나랑 같은 상황을 겪는(겪을) 한인". 검색으로 들어와 "어 이거 내 얘기네" 하고 끝까지 읽게 만드는 게 목표.
(About 페이지 기준: "실용 정보 + 실제 경험 기반 해결 방법")

---

## 1. 보이스 & 톤 — **신버전(B) 표준**

기준 모델: `california-dmv-vehicle-registration-address-change` (DMV 삽질기).
이 글처럼 쓰면 맞다. 아래는 그걸 규칙으로 뽑은 것.

### 1-1. 문체
- **평서체 반말 문어체**: `~했다 / ~이다 / ~한다`. **존댓말(`~습니다`) 금지** (본문 한정. About/정책 페이지는 예외로 존댓말).
- **1인칭 "나/내"**. 가족은 "아이들", 아내는 **"와이프"**(캐주얼 허용). 아빠 시점.
- 문장은 **짧고 끊어서**. 한 문단 2~4문장. 긴 만연체 금지.
- 감정·태도를 드러낸다: "정말 지옥이었다", "허무했다", "이 고생을 했다. 다음엔 안 그럴 거다."
  - 단, 자극적 낚시가 아니라 **실제 겪은 감정**. 과장 금지.

### 1-2. 정직성 (E-E-A-T 핵심)
- **단점을 반드시 솔직하게 적는다.** 예: "누룽지를 긁어내기는 다소 어렵다", "기대했던 것만큼 디파짓을 돌려받지는 못했다."
- 무조건 칭찬/추천 금지. 좋은 건 좋다고, 아쉬운 건 아쉽다고.
- **구체적 근거**: 실제 가격, 모델명(LHTAR0609), 정확한 지명, 시간/절차. 두루뭉술 금지.
- **관계·경력 과장 금지.** 주인과 아는 사이 아니면 "단골" 같은 표현 X → "자주 찾는 곳 / 즐겨 찾는 / 몇 번을 다시 찾게 된 집"처럼 일방적 빈도만. 없는 관계 있는 척 X.
- **경험 vs 공개 사실**: 맛·느낌·평가는 사용자 것만(지어내기 X). 위치·주소·시그니처/추천 메뉴 등 공개 사실은 **검색해서 채운다**. 단 **소스마다 다르거나 불확실한 것(영업시간 등)은 확정적으로 쓰지 말고 생략**(disclaimer가 커버).
- **가격**은 공식/실제 기준만(배달앱 마크업 가격 X), 불확실하면 생략.

### 1-3. 독자 배려
- 글 목적(왜 쓰는지·누구에게 도움 되는지)을 도입부에서 밝히되, **매번 그 글에 맞는 새 문장으로.** 정해진 문구를 복붙하지 않는다.
  - ⚠️ **`~분들 시간 아끼시라고 / 헤매지 마시라고 … 겪은 그대로 적어둔다`는 이미 과사용된 템플릿이다. 그대로 쓰지 말 것.** 여러 글에 같은 문형이 반복되면 그게 바로 AI 티다.
  - 대신 그 글의 **구체적 장면·감정에 붙여** 쓴다. 예: (은행) "그래서 첫 계좌를 어떻게 텄는지, 뭘 챙겨 갔고 어디서 막혔는지 남겨둔다." / (유틸리티) "이 글은 이삿날 불 켜진 집에 들어가려면 뭘 먼저 신청해야 하는지 내가 헤맨 순서 그대로다." / (크레딧) "카드 거절 문자로 시작한 사람으로서, 760점까지 온 순서를 그대로 건넨다."
  - 도입 목적 선언뿐 아니라 **닫는 문장·소제목·연결어도 글마다 달라야 한다.** 틀에 박힌 반복이 보이면 편집자 눈 패스에서 깬다(→ §2.5).
- 마지막은 **독자를 위한 실용 정리/추천**으로 닫는다.
- **한인 독자 맞춤 정보를 놓치지 말 것.** OC 한인 타깃이라 **"한국어 되는 곳인가 / 한국분이 운영하나"**가 은근히 중요한 실용 정보다(영어로 서비스 설명 부담). 병원·정비·업체 후기에서 **사실이면** 본문에 자연스럽게 + `정리하면`에 `한국어: 가능` 식으로 넣는다.

### ✅ 보이스 결정 (확정)

- (a) **전부 B로 통일.** 구버전(A)의 정갈한 후기체는 다듬어지지 않은 초기 글일 뿐, 지향점이 아니다. 새 글은 무조건 B(DMV 삽질기) 톤.
- (b) **캐주얼 호칭 유지.** "와이프", "삽질" 같은 자연스러운 구어 허용 (억지 격식 금지).

---

## 2. 글 구조 (템플릿)

**글 유형(category)에 따라 구조가 다르다. 여기서 사람 느낌이 갈린다.**

### 후기(review) — 충분히 길게 + 문장형 소제목, 끝은 "정리하면" 블록.
기준: 모찌·하코·쿠쿠 글. **소제목 자체는 문제 없다** — AI 티는 "얇은 섹션(1~2문장)을 잘게 쪼갤 때" 난다. **소제목마다 실속 있는 문단 2~3개(각 2~4문장)**를 붙이면 오히려 잘 읽힌다. 충분히 못 풀면 소제목을 합쳐라. 본문 **2,000자 지향**.
```
[도입] 상황 + 결론 살짝 + 왜 쓰는지 (한 문단) + 메인 사진
[본문] 주제마다 [문장형 소제목] + 그 주제를 2~4문장으로 충분히 푼 문단 2~3개.
       소제목 예: "흑임자 빙수 — 많이 안 달고 부드럽다", "자리는 있는 편, 다만 주말엔 다르다". 사진 [사진: ] 최대 2.
[정리하면] 실용 정보를 라벨 블록으로 (본문 소제목이 아니라, 끝의 요약 1개):
       <h2>정리하면</h2><ul>
         <li>위치 : 주소 (랜드마크 옆)</li>
         <li>영업시간 : ...</li>
         <li>추천 메뉴 : ...</li>
         <li>주차/대기 : ...</li>
         <li>가격 : ...선</li>
       </ul>
[마무리] 독자용 추천 한두 문장.
[disclaimer] "이 글은 내가 직접 방문한 경험을 바탕으로 적은 것이다. 메뉴·가격·영업시간은
       시기에 따라 바뀔 수 있으니, 방문 전 확인하길 권한다." (내용에 맞게 변형)
```
- 본문 소제목은 **실속 있게 받쳐줄 때만**(각 2~3문단). 끝의 **`정리하면` 라벨 블록**은 기존 글 표준 포맷 — 각 항목은 **`<li><p><strong>라벨</strong>: 값</p></li>`**(라벨 볼드). 이건 본문 소제목이 아니라 맨 끝 요약 1개.
- **본문에 번호(1.2.3.) 리스트로 정리하지 않는다.** 요약은 위 `정리하면` 라벨 블록으로.
- **표(비교·대응표 등)는 반드시 `<table>` HTML로.** 마크다운 표(`| a | b |`)나 셀을 이어붙인 텍스트는 렌더가 안 된다(이 블로그는 마크다운 파싱 X). 구조: `<table><thead><tr><th>…</th></tr></thead><tbody><tr><td>…</td></tr></tbody></table>`. `.prose`가 스타일링하고 모바일에선 가로 스크롤된다. 에디터도 표 삽입/편집 지원(툴바 `▦`).
  - **대응/등급 열은 색 배지로**: 셀 값을 `<span class="lvl lvl-1">매우 가까움</span>` 형태로. 5단계 — `lvl-1` 진초록(매우/가장 가까움) · `lvl-2` 연초록(가까움·큰 분류상) · `lvl-3` 주황(유사 대응) · `lvl-4` 회색(절단법 따라 다름) · `lvl-5` 빨강(1:1 단정 금지). (에디터 `LevelBadge` 확장이 편집에도 배지를 보존한다.)
- disclaimer는 **`<blockquote>`로 감싸서**(세로바+회색 렌더) 기존 글 문구 그대로 맨 끝에. 가격 등 공개정보 썼으면 필수.

### 정보(how-to) — 구조 OK.
기준: DMV 글. 단계·항목이 있으니 소제목과 번호 리스트가 자연스럽다.
```
[도입 후크] 상황 + 결론 살짝  예) "결론부터 말하면 알고 나면 허무한데 알아내기까진 지옥이었다."
[본문] 문장형 소제목으로 단계를 끊는다. 사진은 [사진: ]
[정리] 숫자 리스트로 실용 요약 (1. 2. 3.) — 정보글엔 OK
```

### 공통 규칙
- **소제목을 쓸 땐 "말하듯 이어지는 문장"으로.** 목차 라벨 금지.
  - ❌ "라벨 — 부연" 템플릿: `뭘 먹었나 — 돈까스 3종`, `맛 — 일식과 한식 중간` (em-dash로 라벨:부연 나누기 금지)
  - ✅ 문장형: `일식과 한식 중간쯤 되는 돈까스`, `굳이 아쉬운 점은 사람이 많다는 것`, `삽질의 절정, 그리고 허무한 정답`
  - **핵심 구분(em-dash 자체는 금지가 아니다)**: 소제목 앞부분이 **범주/목차 라벨**이면 ❌, **구체적 대상/장면**이면 ✅.
    - ❌ `가격대 — 티셔츠 20~100달러`, `결과 — 티도 안 나게 고쳐졌다` (앞이 '가격대'·'결과' = 범주 라벨)
    - ✅ `흑임자 빙수 — 많이 안 달고 부드럽다` (앞이 '흑임자 빙수' = 구체적 대상) — 이 형태는 권장
- **⚠️ 문장형이라고 다 좋은 게 아니다 — 소제목 리듬을 섞어라 (AI 티의 핵심 원인).** 소제목이 전부 "설정→반전/펀치라인" 한 가지 가락(`~다니까 ~달라졌다`, `~인데 ~달랐다`, `~뿐인데 ~시작했다`, `~골랐다 — ~없었다`)으로 통일되면, **문장형이어도 AI가 재치 부린 티가 확 난다.** 사람은 소제목 여섯 개를 그렇게 균일하게 못 쓴다.
  - 기본값 = **짧고 담백하게**(대체로 8~13자, 길어도 15자). 독자는 소제목에서 재치가 아니라 "여기 무슨 얘긴지"를 본다.
  - **형태를 섞는다**: ① 담백한 평서(`지점마다 말이 다르다`) ② 결론/답 먼저(`SSN 없어도 계좌는 만들 수 있다`) ③ 가끔 질문(`어느 은행을 골라야 하나`) ④ 장면 하나(`우편함을 매일 열어봤다`) ⑤ 이유/주제형(`은행부터 만들라는 이유`).
  - **"반전 펀치라인"·감정 마무리는 글 전체에서 한 번이면 충분.** 매 소제목마다 넣지 않는다. em-dash `— 부연`도 **한 글 최대 1회**(연속 사용 금지).
  - 담백한 평서 ≠ 목차 라벨: `지점마다 말이 다르다`(문장 ✅) vs `은행 선택`(라벨 ❌). **짧게 쓰되 라벨로 떨어뜨리진 말 것.**
- 분량: 본문 **최소 1,500자 이상** (평균 1,500~2,500, 정보 많으면 3,000자+). 못 채우면 덜 푼 것.
- **결론/핵심을 앞쪽에 먼저** 던지고 상세는 뒤에.

### ⚠️ 풀어쓰기 (제일 중요 — 단답 금지)
불릿으로 받은 사실을 **그대로 나열하면 안 된다.** 기존 published 글(쿠쿠·다미·DMV) 밀도가 기준이다. 각 포인트를 **최소 2~4문장**으로 풀어라. 사실 하나를 받으면 그 주변에:
- **맥락/이유** — 왜 그게 좋았/아쉬웠는지, 왜 그렇게 느꼈는지
- **감각/장면** — 먹을 때·쓸 때의 구체적 느낌 (바삭함, 개운함 등)
- **망설임/솔직함** — "이건 순전히 내 느낌이라…", "기분 탓일 수도 있지만…" 같은 정직한 톤
- **독자 연결** — "이럴 때 이러면 좋다" 식 상황 제안

을 붙여 **흐르는 문장**으로 만든다. **핵심: 사실은 새로 지어내지 않되(정직성), 그 사실을 겪은 경험·이유·감각은 풀어서 서술한다.** 이게 "불릿 요약"과 "실제 글"의 차이다.

예) 받은 사실: "양배추 샐러드 양 많음"
- ❌ 단답: "양배추 샐러드가 푸짐하다."
- ✅ 풀어쓰기: "사이드로 나오는 양배추 샐러드도 양이 꽤 많다. 돈까스집에서 샐러드가 구색만 갖춰 나오는 경우도 많은데 여기는 인심이 후한 편이다. 튀김을 계속 먹다 보면 슬슬 느끼해지는데, 그때 아삭한 샐러드를 곁들이면 입안이 개운해져 끝까지 물리지 않는다."

---

## 2.5 편집 심화 (편집자 눈 · 정보 층 · 상호 공개)

### 편집자 눈 패스 — 1차 초안 그대로 저장 금지
다 쓴 뒤 냉정하게 다시 보고 약한 곳을 **다시 쓴다.**
- **도입**: 첫 두세 문장이 상황+감정+왜 쓰는지로 끌어당기나?
- **섹션 깊이**: 2문장으로 얕게 끝냈나? 맥락·비교·감각·이유를 붙여 4~6문장으로.
- **전환**: 뚝뚝 끊기나? 앞 문단을 받는 연결("그런데·무엇보다·다만·사실")로 흐르게.
- **마무리**: 밋밋한가? 겪은 걸 되짚고 독자에게 한마디 남기며 닫는다.
- 소리 내 읽듯 다시 보고 **AI 나열 티를 사람 말투로.**
- **크로스-글 템플릿 사냥 (필수)**: 다른 글에도 똑같이 있을 법한 문장을 이 글 전용으로 다시 쓴다. 특히 ①도입 목적 선언(`~분들 …라고 … 적어둔다`) ②마무리 상투구 ③같은 소제목 리듬 ④`결론부터 말하면` 같은 반복 훅. **여러 글에 공통 패턴이 보이면 그게 AI 티다** — 한 편만 보면 괜찮아도 모아 놓으면 어색해진다.
- 기준선 = `irvine-mochi-dessert-cafe-review`, `buena-park-auto-glass-repair-review`. 그 밀도에 못 미치면 덜 된 것.

### 정보 층 — 경험 + 유용한 일반 정보 한 겹 (고품질의 결정적 차이)
개인 경험만 나열하면 얕다. 독자가 결정하는 데 도움되는 **일반 정보를 검색해 한 겹 얹는다**(경험을 지어내는 게 아니라 공개 지식을 더함).
- 예(자동차 유리): "돌빵 수리 vs 교체"를 크기·위치·깊이 기준으로 + "미룰수록 금 번짐(온도)" + "테슬라가 비싼 건 앞유리 카메라 재보정".
- **반드시 일반 정보로 프레이밍**("알아보니…", "~라고 한다") + 내 경험과 엮어서("내 경우엔 깊었는데도 수리됐다") 흐르게.
- 놀라운 수치엔 "왜"를 붙인다. 이 층이 있으면 = high value content(AdSense·SEO 직결). 정보성 주제일수록 필수.

### 상호·업체 공개는 선택
- 도움되면 밝힌다(상호·주소·시그니처).
- 홍보처럼 보일 것 같으면 상호를 빼고 "특정 가게 홍보 아니라 안 적었다, 위치 궁금하면 댓글·메일로 문의"(자동차유리 방식). 어느 쪽이든 사용자 의도 확인.

---

## 3. 제목 규칙 (SEO)

현재 잘 되는 패턴 두 가지:

1. **실용형**: `[대상] 후기 | [키워드 담은 실용 부제]`
   - 예: "Irvine Company 이사 후기 | 프리무브아웃 점검, 청소비, 디파짓 정산 경험"
2. **후킹형**: `[대상] 후기 – [서사/공감 부제]`
   - 예: "밥솥 하나 바꿨을 뿐인데 밥 먹는 즐거움이 달라졌다"
   - 예: "…주소가 안 바뀌어서 한 달을 헤맸다 (배우자 명의 차량 주의)"

규칙:
- **키워드를 제목 앞쪽에** 배치 (지명·제품명·행위).
- 지명·브랜드는 **영문 원명 + 한글 병기** 유연하게 (Crystal Cove(크리스탈 코브)).
- "후기"는 핵심 키워드 — 후기성 글엔 넣는다.
- 괄호로 **구체 정보/주의**를 덧붙이면 CTR↑ (예: "(주차 꿀팁)", "(배우자 명의 차량 주의)").
- ✅ **구분자 이원화 확정**: 실용형(키워드 나열)은 `|`, 후킹형(서사 부제)은 `–` 또는 괄호.

---

## 4. 메타 디스크립션 규칙 (= 요약/excerpt)

**핵심: "요약(excerpt)" 필드가 곧 메타 디스크립션이다.** 지금도 잘 되고 있다 — 25편 전부 요약 채워져 있고 평균 141자.

작동 방식 ([app/blog/[slug]/page.tsx](app/blog/[slug]/page.tsx)):
- 검색결과 설명 / OG / 트위터 카드 = `meta_description || excerpt`
- 즉 **요약만 잘 쓰면 SEO 설명은 비워도 된다.** `meta_description`(SEO 설명 필드)은 요약과 *다른* 문구를 내보내고 싶을 때만 쓰는 override.

규칙:
- **요약(excerpt)은 필수.** 빈 값 금지. (이미 100% 지키는 중)
- excerpt 길이 **140~160자** 권장 (검색결과 잘림 방지). 인제스트는 **80~200자 허용**(§13).
- **타깃 키워드 + 이 글이 답하는 것 + 실경험 신호**를 담는다.
- 클릭 유도 문장 1개 포함. 본문 첫 문장 복붙 금지(따로 쓴다).
- 요약은 **블로그 목록 카드 미리보기로도** 쓰이니(2줄), 첫 문장이 매력적이어야 함.
- **AI 초안 생성 시엔 `meta_title`(SEO 제목, 60자 내외)·`meta_description`(SEO 설명, **155자 이내** — excerpt보다 짧게)도 작성해서 넣는다.** (수동 작성 시엔 요약 폴백에 기대도 되지만, 자동 생성은 SEO 필드까지 채우는 걸 기본으로.)
- ⚠️ **excerpt ≠ meta_description.** 둘 다 채울 거면 **서로 다른 문장**으로 쓴다. **meta를 excerpt에 그대로 복붙 금지** — 똑같이 복사할 거면 차라리 `meta_description`을 비워 excerpt 폴백에 맡긴다. (`/api/drafts`가 이 복붙을 400으로 잡는다 → §13.)

예시(DMV 글):
> "캘리포니아 DMV 차량 등록 갱신 중 주소가 안 바뀌어 한 달을 헤맨 실경험. 배우자 명의 차량은 본인 계정에서만 처리되는 이유와 해결 절차, 이사 후 꼭 바꿔야 할 주소 3가지를 정리했다."

---

## 5. 태그 규칙 ✅ (공백 통일 확정)

**표기: 띄어쓰기는 공백. 하이픈 금지.** (2026-07-15 확정 + 기존 태그 정규화 완료 — 마이그레이션 `20260715124000_normalize_tags_hyphen_to_space`.)
왜: 이 사이트는 태그를 **쉼표로** 쪼갠다(공백 아님). 태그 전용 URL도 없다. 그래서 `미국 백투스쿨`은 공백이 있어도 태그 하나. 하이픈은 오히려 `Set` 중복 집계만 어지럽혔다.

- **개수**: 글당 **4~6개** 고정
- **표기**: 공백 유지 — 예: `미국 백투스쿨`, `백투스쿨 준비물` (하이픈·언더스코어 금지)
- **언어**: 검색량 따라 한글 우선, 필요시 영문 브랜드/지명 1~2개 허용
- **구성 공식(추천)**: `[지역] + [대상/브랜드] + [행위/카테고리] + [롱테일 1개]`
  - 예(꿀돼지): `얼바인 맛집`, `honey pig`, `한국식 삼겹살`, `OC 한인 맛집`

---

## 6. 클러스터 (토픽 허브)  — 항상 지정

**운영 원칙: 얇은 주제는 묶고, 5편 이상 쌓이면 쪼갠다.** (4편 이하 얇은 허브는 새로 만들지 않는다 — 3~4편짜리는 사용자·구글 둘 다 약하게 본다.) **5편 미만으로 얇게 분리된 클러스터는 상위 버킷으로 되돌린다**(예: `car` 3편 → `settlement` 병합, 2026-07-21).

현재 클러스터 6개 (`car` → `settlement` 병합, 2026-07-21):

| key | 제목 | 담는 것 | 분리 예정(5편+) |
| --- | --- | --- | --- |
| `settlement` 🧭 | 미국 정착 실무 가이드 | 은행·송금·DMV·이민 서류·**자동차·병원** 등 정착 실무 | 자동차/의료가 각 5편 되면 분리 |
| `housing` 🏠 | 미국 렌트·이사 가이드 | 렌트, 이사, 디파짓, 거주 후기 | — |
| `kids` 🎒 | 미국 자녀교육·학교 가이드 | 학원, 백투스쿨, 학교 행정·검사 | — |
| `food` 🍽️ | 미국 맛집·집밥 가이드 | 맛집, 집밥, **주방 제품 후기** | 제품후기 5편+ 되면 분리 |
| `travel` ✈️ | 미국·해외 여행 가이드 | 여행지, 근교 나들이, 로컬 이벤트 | — |
| `shopping` 🛍️ | 미국 쇼핑·생활정보 | 세일, 중고거래, 생활 꿀팁 | — |

규칙:
- 글 쓸 때 **무조건 클러스터 1개 지정** (미분류 금지).
- 애매하면 가장 가까운 것에. 얇은 주제는 위 표의 상위 버킷(주로 `settlement`)에 넣어두고, **5편째가 나오면** 새 클러스터로 분리.
- **왜 5편?** 3~4편짜리 허브는 토픽 권위가 약하다(사용자·구글 둘 다). **적고 두꺼운 클러스터가 강하다.**
- 분리 시: `clusters`에 insert → 해당 글 `posts.cluster` 재배정 (마이그레이션 파일로). 옛 허브가 색인됐을 수 있으니, 클러스터를 **없애거나 병합할 땐 `/guides/[old]` → `/guides/[new]` 리다이렉트 필수**(GSC 404 방지 — 슬러그 변경과 같은 처리).

---

## 6.5 내부 링크 & 기둥글 (코너스톤) — 글끼리 엮기

글을 따로 쌓기만 하면 서로 남처럼 논다(2026-07-20 점검: 정착 클러스터 7편이 상호 링크 **0개**였다). **관련 글끼리 본문에서 링크로 엮어야** 색인·체류·SEO가 산다.

- **문맥(in-body) 링크가 핵심.** 하단 자동 위젯(RelatedPosts·클러스터 박스)은 이미 붙는다 — 실제 힘은 **본문 문장 안의 자연스러운 링크**다. 관련 주제를 **실제로 언급하는 지점**에만 건다.
- **허브-스포크.** 클러스터마다 기둥(코너스톤) 글 하나를 정하고(예: 정착 = `us-bank-account-no-ssn` 은행 계좌), 관련 글은 기둥으로 링크를 모으고 기둥은 다음 단계로 내보낸다(계좌 → 크레딧, 계좌 → 유틸리티).
- **앵커는 대상 글의 주제어로, 문장 흐름 안에서.** 예: `앞으로의 <a href="/blog/us-credit-score-from-scratch">크레딧 관리</a>를 위한 첫 단추`. "여기 클릭"·억지 삽입 금지.
- **글당 1~3개.** 과하면 스팸 신호. 정말 관련된 것만.
- **형식**: 내부 링크는 `<a href="/blog/슬러그">앵커</a>` — external과 달리 `target="_blank"`·`nofollow` **붙이지 않는다**(같은 탭, follow).
- **"함께 보면 좋은 글" / 시리즈 블록**: 걸 거면 **이미 발행된 글만.** 미발행 글로 데드링크 걸지 말 것 — 아직이면 그 항목/블록을 빼고 발행 후 추가한다.
- **토픽이 다르면 억지로 엮지 않는다.** 같은 클러스터라도 결이 다른 글(예: 정착 클러스터의 치과 후기·비자 불러틴은 돈/셋업 흐름과 별개)은 문맥 링크 대상이 아니다 — 자동 위젯이 느슨하게 커버한다.

---

## 7. 카테고리 = 글 유형 (후기 / 정보)

**역할 분리: cluster = 주제(topic), category = 글 유형(type).** 둘이 안 겹치게.
(2026-07-15 확정, 마이그레이션 `20260715123000_normalize_categories`. 이전 17종 뒤죽박죽 → 2종.)

- **후기** — 특정 제품·장소·서비스 직접 경험 평가 (기본값, 대부분)
- **정보** — 방법·비교·리스트 (how-to / 비교 / 준비물 등). 예: DMV 해결법, 송금 방법, 앱 4개 비교, 백투스쿨 리스트

규칙:
- 값은 **`후기` / `정보` 두 개만.** 새 값 임의 생성 금지.
- 헷갈리면 **후기가 기본**. "이거 살까/갈까?"에 답하면 후기, "어떻게 하지?"에 답하면 정보.
- ✅ 에디터 카테고리 필드는 **드롭다운(`후기`/`정보`)** 으로 고정됨 — 자유입력 불가라 값이 다시 섞일 수 없음.
- 참고: category는 블로그에서 필터·라벨로 노출되지만 cluster와 축이 겹쳐 **적극 활용도는 낮음.** 최소한 깔끔하게만 유지.

---

## 8. 이미지 표준

- **직접 찍은 사진 우선.** (에이전트가 못 채우는 부분 = 여기가 사람의 가치)
- 본문 첫 이미지가 **커버로 자동 설정**됨(에디터 로직) → 첫 이미지에 대표성 있는 컷.
- **커버가 마땅찮으면(명함·영수증뿐 등) 관련 무료 스톡을 커버로.** 에디터 이미지 검색(Pexels)에서 주제 이미지 하나를 맨 위에, **실물(명함 등)은 본문에**. 커버·썸네일은 장식 헤더라 스톡 OK — 단 "내가 찍은 것"처럼 캡션 달지 않기(정직성은 본문에 적용).
- 설명 필요한 사진엔 **캡션 `▲ ...`** (DMV 글 방식).
- **alt는 에디터가 자동 생성**(`제목 이미지 N`, [RichEditor.tsx](../components/editor/RichEditor.tsx) `generateAlt`) — 제목 키워드가 들어가는 자동 처리라 **초안·발행 시 alt는 신경 쓰지 않아도 됨**. 수동 alt 입력 UI는 없다(현재는 불필요로 판단).
- 사진을 못 찍었거나 **찍었는데 잃어버렸으면 글에 솔직히 적는다**(예: "전후 사진을 찍어뒀는데 어디 갔는지 못 찾겠다, 아쉽다"). 오히려 사람 티 나는 진정성 신호 — AI는 이런 말 안 함.

### 8-1. 저작권 — 쓸 수 있는 것 / 절대 안 되는 것

**쓸 수 있는 것**
- 본인이 직접 찍은 사진
- Pexels / Unsplash 무료 스톡 (상업적 이용 무료, 푸터에 출처 표기)
- Google Maps **임베드 코드**(iframe — 아래 반응형 래퍼로 감쌈)
- 자체 제작 그래픽(SVG/Canva)

**절대 쓰면 안 되는 것**
- Google 지도 **스크린샷**·스트리트뷰 캡처 (크롭·보정해도 안 됨 — 구글 저작물)
- 브랜드 공식 로고·제품 이미지 (COS·Broken Yolk 등 — 상표권)
- 남의 사진 (맛집 블로그·Yelp 등)
- **AI로 보정/재생성한 이미지** — 저작권이 애매해지고 "AI-generated content" 워터마크·메타데이터가 붙을 수 있음
  - ⚠️ **범위**: 이 금지는 **블로그 본문 사진**에 한한다. 소셜용 일러스트 포스터는 AI 생성이 정식 워크플로다(→ `ai-poster-recipe.md`). 둘을 섞지 말 것.

### 8-2. 사진 처리 방법 (PIL/Pillow)

- **보정**: 대비 1.15~1.35, 채도 1.15~1.4, 선명도 1.2~1.3
- **최적화**: 가로 1200~1600px, quality 88~90
- **워터마크 제거**: 하단 4% 크롭. AI 재생성 없이 단순 편집(크롭/밝기)만.
- **⚠️ EXIF 방향 필수**: 폰 사진은 EXIF에 회전 정보가 들어 있어, 편집 전 `ImageOps.exif_transpose()`를 먼저 적용하지 않으면 사진이 눕는다. (카카오톡으로 받은 사진에서 특히 발생)

### 8-3. Google Maps 임베드 (반응형)

장소 후기엔 지도 스크린샷 대신 임베드를 쓴다:
```html
<div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;border-radius:8px;margin:20px 0;">
  <iframe src="[구글 지도에서 복사한 embed src]"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allowfullscreen loading="lazy" referrerpolicy="strict-origin-when-cross-origin" title="장소명 위치"></iframe>
</div>
```

### 8-4. AI 이미지에 대한 판단

- 구글은 AI 생성 이미지 자체엔 순위 페널티를 주지 않는다("어떻게 만들었나가 아니라 품질").
- 다만 저작권 소유가 애매하고 생성 도구가 "AI-generated content" 메타데이터를 심을 수 있어 **AdSense 블로그 본문엔 권장하지 않는다.**
- 특히 **공식 사이트 화면을 AI로 재생성한 이미지**는 원본 저작권이 그대로 남으므로 금지.
- 우선순위: **본인 사진 > Pexels/Unsplash 스톡 > 직접 만든 그래픽.**

---

## 9. 발행 전 체크리스트 (→ `ondostory-seo-check` 스킬 원천)

- [ ] 제목: 키워드 앞쪽 + 후기/후킹 패턴
- [ ] 도입부: 결론 살짝 + 공감 후크
- [ ] 소제목(H2/H3)으로 구조화
- [ ] 마지막에 번호 리스트 정리 + 독자용 추천
- [ ] 단점/솔직한 코멘트 최소 1개 (E-E-A-T)
- [ ] 구체 정보(가격·모델명·지명·절차) 포함
- [ ] **요약(excerpt) 140~160자 작성** (= 메타 디스크립션. 빈 값 금지. SEO 설명 필드는 대개 비워둠)
- [ ] 태그 4~6개, 표기 규칙 준수
- [ ] **클러스터 지정** (미분류 금지)
- [ ] 카테고리 지정
- [ ] 커버 이미지(첫 이미지) 대표성 확인
- [ ] 슬러그: 영문 소문자·하이픈, **3~5단어(최대 5, 짧을수록 좋음)**. 발행 시점에 사람이 최종 확정. **배포된 글의 슬러그는 함부로 바꾸지 않는다**(색인·유입 끊김 → 불가피하면 `next.config.ts`에 301/308 리다이렉트 필수)

---

## 10. 사이트·인프라 기본

- **사이트**: www.ondostory.com — 미주 한인(OC) 대상 한국어 생활정보 블로그
- **스택**: Next.js(App Router) + Supabase(자체 CMS), Cloudflare Workers 배포(OpenNext)
- **sitemap**: `app/sitemap.ts` = `force-dynamic` → 새 글 자동 반영(손댈 필요 없음). **RSS**: `/rss.xml` 정상
- **AdSense**: pub-3702232308312218
- **분석**: Google Search Console(www 속성) + Cloudflare Web Analytics
- **필수 페이지**: About / Contact / Privacy Policy / Terms / Disclaimer 완비
- **SNS 자동화**: Zapier 무료 플랜으로 RSS → 페이스북 페이지(ondostoryofficial) 자동 게시. 핀터레스트(pinterest.com/ondostory)도 운영

---

## 11. 유입·배포 전략 (현실 인식)

- **검색 유입이 정답이지만 느리다** — 신규 사이트는 6개월~1년. 롱테일(구체적 지역+브랜드)이 먼저 오른다. 지름길은 없고, 좋은 글을 꾸준히 쌓는 것뿐.
- **시도했다 접은 것**: OC 한인 커뮤니티(오씨코리아 등)는 광고·업소록 중심이라 개인 정보글 자리가 마땅치 않음. 페이스북 페이지는 팔로워가 적어 유기적 도달 1~5%. 커뮤니티 직접 만들기는 닭-달걀 문제라 보류.
- **SNS 자동화(구축됨)**: Zapier로 RSS→FB 자동 게시. dlvr.it은 FB 연동 버그, IFTTT는 RSS 트리거 유료화로 탈락. 팔로워가 적어 당장 효과는 없고 "기반"으로만 유지.
- **배포 시 주의(AdSense)**: 소셜엔 **후크 + 대표 이미지/포스터 + 링크**만 올려 블로그로 유입시킨다. 본문 전문을 그대로 올리면 독자가 소셜에서 다 읽어 사이트 방문(=광고 노출)이 안 생긴다.
- **결론**: 조급해하지 말 것. 좋은 글 꾸준히 + 6개월~1년. 배포 실험은 여력 될 때.

---

## 12. 주제 발굴 — 뭘 쓸까

무슨 글을 쓸지 막힐 때 소스:

1. **직접 겪은 경험 (기본).** 정착·생활에서 부딪힌 일, 산 것, 간 곳, 해결한 절차. 이게 이 블로그의 뿌리이자 E-E-A-T의 원천이다.
2. **이슈 해설 (뉴스를 소재로).** 미국/한국 뉴스에서 **OC 한인 생활에 실제로 영향 주는 변화**를 골라, **번역이 아니라** 내 경험·관점·실용 함의를 얹어 새로 쓴다.
   - **번역·전재 금지, springboard로만.** 사실은 인용(+ 출처 링크), 분석·경험은 내 것 → 저작권·AdSense 안전 + E-E-A-T 유지. 기사 통째 번역은 **2차 저작물 침해 + 저품질 신호**라 도메인 전체를 깎는다.
   - ❌ `캘리포니아 최저임금 인상 (기사 번역)`
   - ✅ `캘리포니아 최저임금 올랐다 — 우리 집 생활비·렌트에 실제로 뭐가 바뀌나` (내 관점 + 정착 실용)
   - ✅ `새 이민 정책 발표 — 배우자 비자 입장에서 뭘 봐야 하나` (내 경험 각도)
   - `category = 정보`, **관련 클러스터에 배치**(별도 "뉴스" 탭은 만들지 않는다). 태그에 `생활 이슈` 정도.
   - **3편 이상 쌓여 형식이 검증되면** §6 원칙대로 `이슈`/`시사` 클러스터로 승격 검토(그때 탭도). 그 전엔 기존 클러스터 안에서 소화.

---

## 13. 발행 인제스트 — `/api/drafts`

외부에서 만든 글을 draft로 넣는 엔드포인트. claude.ai 등은 **글만 쓰고**, 실제 호출은 **네트워크 열린 곳**(Claude Code·터미널·GitHub Action)에서 한다 (claude.ai는 ondostory로 직접 호출 불가).

- **`POST https://www.ondostory.com/api/drafts`**
- **인증**: `Authorization: Bearer <DRAFT_API_TOKEN>` (wrangler 시크릿 — `wrangler versions secret put`로 설정, OpenNext가 버전 배포라 클래식 `secret put`은 막힘)
- **`status`는 서버가 무조건 `draft`로 강제** — 이 엔드포인트로는 **발행·색인 불가**. 발행은 사람이 `/admin/drafts`에서.

**payload (JSON):**
```json
{
  "title": "제목 (검색어 앞)",
  "slug": "kebab-case-slug",
  "content": "<p>본문 HTML</p><h2>소제목</h2>...",
  "excerpt": "요약 140~160자",
  "tags": ["태그1", "태그2", "태그3", "태그4"],
  "cluster": "settlement",
  "category": "정보",
  "meta_title": "SEO 제목",
  "meta_description": "SEO 설명 140~160자"
}
```

**검증 (안 맞으면 400 + 이유 배열):**
- 필수: `title` · `slug` · `content` · `excerpt`
- `slug`: 소문자·숫자·하이픈(kebab), 6단어 이하
- `excerpt`: 140~160 권장(80~200 허용)
- `category`: `후기`/`정보`만 (선택)
- `cluster`: 존재하는 key만 (선택) — §6 표 참조
- `tags`: 배열 1~8개(권장 4~6). **표기는 공백, 하이픈 금지** (§5 — 마이그레이션으로 정규화된 규칙)
- `meta_description`: 155자 이내 권장(최대 200). **excerpt와 똑같으면 400**(복붙 금지 → §4). 비우면 excerpt가 폴백.
- `content`: **HTML 문자열**(마크다운 X). `[사진: ...]` 마커는 텍스트 그대로 둔다(img 태그 넣지 않음).
- **맨 끝 disclaimer는 `<blockquote><p>...</p></blockquote>`로 감싼다** (§2 하우스 포맷 — 다른 글과 통일. 그냥 `<p>`로 두지 말 것).
- **표는 `<table><thead>…</thead><tbody>…</tbody></table>` HTML로** 준다. 마크다운 표(`| a | b |`)로 주면 텍스트가 한 줄로 뭉개진다(마크다운 파싱 X). §2 참조.
- 성공: `201 {ok, id, slug, status:"draft", admin_url}` → `/admin/drafts`에 뜬다.

**주의**: `status`는 payload에 넣어도 무시된다(항상 draft). **이미지 파일은 안 받는다** — 본문엔 `[사진:]` 마커만, 실제 이미지는 사람이 에디터에서 채운다(§8).

**내부 링크는 리뷰 시점에 추가한다(§6.5).** claude.ai는 라이브 발행글 목록을 모르므로 인제스트 payload엔 내부 링크가 없어도 된다 — draft 검토 때 관련 발행글로 향하는 앵커를 본문에 심고(같은 클러스터 우선), 발행 시 상대 글에서 역링크(허브-스포크)도 건다. **인제스트 글을 그냥 발행하지 말고 내부 링크부터 확인**할 것.

---

## 부록 A. 보이스 대조 (같은 소재, A→B 개선 감각)

**A (구버전, 개선 전):**
> "이번 Dami Dental 방문은 처음부터 끝까지 전반적인 만족도가 매우 높았다. …여러 부분에서 좋은 인상을 받았다."

**B (신버전 지향):**
> 결론부터. 거리는 멀지만 다시 간다. 이유는 셋이다.
> ## 1. 접수부터 안 급하다  …
> ## 2. 필요 없는 치료는 안 권한다  …
> (소제목 + 근거 + 솔직 + 리스트)
